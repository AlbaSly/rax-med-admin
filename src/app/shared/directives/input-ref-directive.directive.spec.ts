import { InputRefDirectiveDirective } from './input-ref-directive.directive';

describe('InputRefDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRefDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
