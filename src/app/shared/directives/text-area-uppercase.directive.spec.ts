import { TextAreaUppercaseDirective } from './text-area-uppercase.directive';

describe('TextAreaUppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAreaUppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
